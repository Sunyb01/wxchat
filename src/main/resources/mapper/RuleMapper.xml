<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scblock.wxchat.mapper.RuleMapper">
<!--    -->
    <select id="findRulesById" resultType="com.scblock.wxchat.entity.Rule">
        select rulename from rule where user_id = #{id}
    </select>
</mapper>